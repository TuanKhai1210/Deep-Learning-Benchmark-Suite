# Local datasets — not committed

Generated raw/downloaded/processed data lives here and is ignored by Git.
Use the preparation command once the data pipeline is implemented.
Fixed split indices and train-only normalization metadata belong in the tracked
`configs/a1/splits/` directory, not here.
