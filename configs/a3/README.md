# Reserved for Assignment 3

Define modality-specific data/model/evaluation contracts after proposal approval.
Do not force multimodal data into the A1 image-only Batch schema. Add
`src/dlbench/a3/` when the approved task is known; reuse common helpers selectively.

