# Exploration only

Optional EDA notebooks may live here once created; keep outputs small and clear.
Reusable logic belongs in `src/dlbench/`, imported into the notebook.
Training/evaluation must also run via the package CLI, not only notebook cell order.
No empty or fake executed notebook is provided.

